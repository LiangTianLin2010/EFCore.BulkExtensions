name: Publish to NuGet

on:
  release:
    types: [published]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write
      contents: read

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: '8.0'

    - name: Restore dependencies
      run: dotnet restore EFCore.BulkExtensions.sln

    - name: Build all projects
      run: dotnet build EFCore.BulkExtensions.sln -c Release --no-restore

    - name: Pack all projects
      run: |
        # 主项目
        dotnet pack EFCore.BulkExtensions/EFCore.BulkExtensions.csproj -c Release --no-build -o out
        
        # 子项目（只打包实际存在的）
        dotnet pack EFCore.BulkExtensions.MySql/EFCore.BulkExtensions.MySql.csproj -c Release --no-build -o out
        dotnet pack EFCore.BulkExtensions.Oracle/EFCore.BulkExtensions.Oracle.csproj -c Release --no-build -o out
        dotnet pack EFCore.BulkExtensions.PostgreSql/EFCore.BulkExtensions.PostgreSql.csproj -c Release --no-build -o out
        dotnet pack EFCore.BulkExtensions.Sqlite/EFCore.BulkExtensions.Sqlite.csproj -c Release --no-build -o out
        dotnet pack EFCore.BulkExtensions.SqlServer/EFCore.BulkExtensions.SqlServer.csproj -c Release --no-build -o out
        dotnet pack EFCore.BulkExtensions.Core/EFCore.BulkExtensions.Core.csproj -c Release --no-build -o out
        # GBase 已移除，因为项目不存在

    - name: List output files
      run: ls -la out/

    - name: NuGet Login (OIDC)
      uses: NuGet/login@v1
      id: login
      with:
        user: ${{ secrets.NUGET_USER }}

    - name: Push to NuGet
      run: dotnet nuget push out/*.nupkg --api-key ${{ steps.login.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json
